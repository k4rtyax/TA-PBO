<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>

<BorderPane xmlns:fx="http://javafx.com/fxml"
            fx:controller="controller.FormPoliController"
            stylesheets="@style.css">

    <!-- TITLE -->
    <top>
        <VBox alignment="CENTER"
              spacing="15"
              styleClass="top-panel">
            <padding>
                <Insets top="20"
                        right="20"
                        bottom="10"
                        left="20"/>
            </padding>
            <Label text="FORM DATA POLI"
                   styleClass="title"/>
        </VBox>
    </top>

    <!-- CONTENT -->
    <center>
        <VBox alignment="CENTER"
              spacing="25">
            <padding>
                <Insets top="10"
                        right="30"
                        bottom="20"
                        left="30"/>
            </padding>

            <GridPane alignment="CENTER"
                      hgap="20"
                      vgap="18">

                <!-- COLUMN WIDTH -->
                <columnConstraints>
                    <ColumnConstraints minWidth="120"/>
                    <ColumnConstraints minWidth="220"/>
                </columnConstraints>

                <!-- ROW 1 -->
                <Label text="ID Poli"/>
                <TextField fx:id="txtId"
                           editable="false"
                           prefWidth="220"
                           GridPane.columnIndex="1"/>

                <!-- ROW 2 -->
                <Label text="Nama Poli"
                       GridPane.rowIndex="1"/>
                <TextField fx:id="txtNamaPoli"
                           prefWidth="220"
                           GridPane.columnIndex="1"
                           GridPane.rowIndex="1"/>

                <!-- ROW 3 -->
                <Label text="Lokasi"
                       GridPane.rowIndex="2"/>
                <TextField fx:id="txtLokasi"
                           prefWidth="220"
                           GridPane.columnIndex="1"
                           GridPane.rowIndex="2"/>

                <!-- ROW 4 -->
                <Label text="Deskripsi"
                       GridPane.rowIndex="3"/>
                <TextArea fx:id="txtDeskripsi"
                          prefWidth="220"
                          prefHeight="80"
                          GridPane.columnIndex="1"
                          GridPane.rowIndex="3"/>

            </GridPane>

            <!-- BUTTON -->
            <HBox alignment="CENTER"
                  spacing="20">
                <Button fx:id="btnSimpan"
                        text="Simpan"
                        onAction="#handleSimpan"
                        styleClass="btn-save"
                        prefWidth="120"
                        prefHeight="40"
                        contentDisplay="LEFT"
                        graphicTextGap="8">
                    <graphic><Label styleClass="icon-mi" text="&#xE161;"/></graphic>
                </Button>

                <Button text="Batal"
                        onAction="#handleBatal"
                        styleClass="btn-cancel"
                        prefWidth="120"
                        prefHeight="40"
                        contentDisplay="LEFT"
                        graphicTextGap="8">
                    <graphic><Label styleClass="icon-mi" text="&#xE5CD;"/></graphic>
                </Button>
            </HBox>
        </VBox>
    </center>

</BorderPane>
